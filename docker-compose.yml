version: '3.8'

services:
  app:
    image: ghcr.io/system-analysis-and-design-2023-sut/server:1.3.0
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
    hostname: sad-server-{{.Task.Slot}}
    ports:
      - "8080:8080"
      - "8081:8081"
      - "8082:8082"

